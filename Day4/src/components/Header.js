import React from 'react';

const Header = () => {
	return (
		<header>
			<span>Click on the background to copy to clipboard</span>
			<h1>Color Palette Generate</h1>
		</header>
	);
};

export default Header;
